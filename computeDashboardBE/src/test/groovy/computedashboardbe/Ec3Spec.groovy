package computedashboardbe

import grails.test.mixin.TestFor
import spock.lang.Specification

/**
 * See the API for {@link grails.test.mixin.domain.DomainClassUnitTestMixin} for usage instructions
 */
@TestFor(Ec3)
class Ec3Spec extends Specification {

    /*
    This space intentionally left blank to indicate that the Ec3 domain class is merely seven
    strings and unless my team's QA argues very persuasively I do not typically write tests
    to make sure that thing.setField() and thing.getField() agree with each other.
     */
}
